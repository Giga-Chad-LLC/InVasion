syntax = "proto3";

package game_schemes;
import "player-schema.proto";
import "bullet-schema.proto";

message GameStateSchema{
  repeated PlayerSchema teammates = 1;
  repeated PlayerSchema enemies = 2;
  repeated BulletSchema bullets = 3;
}